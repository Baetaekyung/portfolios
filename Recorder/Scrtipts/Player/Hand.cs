using UnityEngine;

public class Hand : MonoBehaviour
{
    //private readonly int boolHashData = Animator.StringToHash("HandUp");

    //private Animator _animator;
    //private bool toggle = false;

    private void Awake()
    {
        //_animator = GetComponent<Animator>();
    }

    private void Update()
    {
        //if (Input.GetMouseButtonDown(1))
        //{
        //    if (toggle)
        //    {
        //        //_animator.SetBool(boolHashData, false);
        //        toggle = !toggle;
        //    }
        //    else if (!toggle)
        //    {
        //        //_animator.SetBool(boolHashData, true);
        //        toggle = !toggle;
        //    }
        //}
    }
}
